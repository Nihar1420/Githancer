import type { GapInfo, Timestamp } from '@gtm/common';

/** Find gaps between consecutive timestamps in a sorted list. Phase 2. */
export function findGaps(_timestamps: Timestamp[]): GapInfo[] {
  throw new Error('findGaps not implemented — Phase 2');
}
